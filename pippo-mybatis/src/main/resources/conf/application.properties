application.name    = ${project.name}
application.version = v${project.version}

application.languages = en

server.port = ${application.port}
server.contextPath = /api

# Report Metrics via MBeans for VisualVM, JConsole, or JMX
metrics.mbeans.enabled = true

datasource.driver   = com.mysql.jdbc.Driver
datasource.url      = jdbc:mysql://localhost:3306/microservice
datasource.username = microservice
datasource.password = microservice123
